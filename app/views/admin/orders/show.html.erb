<h3>注文履歴の詳細</h3>
<table>
  <tr>
    <th>購入者</th>
    <td><%= @order.customer.first_name %><%= @order.customer.last_name %></td>
  </tr>
  <tr>
    <th>注文日</th>
    <td><%= @order.created_at.strftime("%Y/%m/%d %H:%M:%S") %></td>
  </tr>
  <tr>
    <th>配送先</th>
    <td><%= @order.shipping_address %></td>
  </tr>
  <tr>
    <th>支払方法</th>
    <td><%= @order.payment_method %></td>
  </tr>
  <tr>
    <th>注文ステータス</th>
    <td></td>
  </tr>
</table>
<table>
  <thead>
    <tr class="bg-light">
      <th>商品名</th>
      <th>単価（税込）</th>
      <th>数量</th>
      <th>小計</th>
      <th>制作ステータス</th>
    </tr>
  </thead>
  <tbody>
    <% @order.order_details.each do |order_detail| %>
      <tr>
        <td><%= order_detail.item_name %></td>
        <td><%= order_detail.add_tax_price %></td>
        <td><%= order_detail.amount %></td>
        <td><%= order_detail.add_tax_price.to_s(:delimited) %></td>
        <td>
          <%= form_with model: @order, url: admin_order_path(order) do |f| %>
          <%= f.submit "更新", class: "btn btn-success" %>
          <%= f.select :status, Product_status.keys %>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<table>
  <tr>
    <th>商品合計</th>
    <td><%= @order_detail.price %><td>
  </tr>
  <tr>
    <th>送料</th>
    <td><%= @order.postage.to_s %></td>
  </tr>
  <tr>
    <th>請求合計額</th>
    <td><%= @order_detail.price += @order.postage.to_s %></td>
  </tr>
</table>
